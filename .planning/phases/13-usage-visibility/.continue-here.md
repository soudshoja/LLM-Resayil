---
phase: 13-usage-visibility
task: 0
total_tasks: 4
status: ready_to_start
last_updated: 2026-03-07T11:59:56.832Z
---

<current_state>
Phase 13 is fully planned and ready to execute. Phase 12 just completed and was merged to prod as v1.12.0. Next action is to run Phase 13 Wave 1 (3 parallel fix tasks).
</current_state>

<completed_work>
## This Session (everything done before handoff)

### Phase 11 — COMPLETE (v1.11.0)
- FAQ + Features pages localized EN/AR (87 translation calls)
- Fixed broken route() references across 7 files (route('docs')→route('docs.index'), route('dashboard')→url('/dashboard'))
- Merged dev→main, deployed prod, tagged v1.11.0
- All 8 public routes return 200 on prod

### Phase 12 — COMPLETE (v1.12.0)
- Bug A: Created routes/v1.php — /v1/ routes now work without /api prefix
- Bug B: Non-issue — models table empty is correct
- Bug C: Created cache + sessions tables via migration
- Streaming fix: 3 root causes fixed in OllamaProxy.php + ChatCompletionsController + .htaccess
- RouteServiceProvider: dual-prefix so both /v1/ and /api/v1/ work
- TrustHosts: added llmapi.resayil.io
- llmapi.resayil.io subdomain: live, DNS-only (no Cloudflare), SSL valid, streaming confirmed
- qwen3-vl:235b-instruct-cloud model: added, credit_multiplier 3.5, context 128k
- Performance tests run: effective limit ~105k tokens, 4 concurrent users confirmed parallel
- Fresh test account created: perftest@llm.resayil.io (50k credits, enterprise)
- Merged dev→main, deployed prod, tagged v1.12.0
- All routes verified on prod

### Phase 13 — PLANNED (not started)
- RESEARCH.md complete
- 13-PLAN.md complete (4 plans, 3 waves)
- STATE.md + ROADMAP.md updated (commit 9d621c5)

### Phase 14 — PLANNED (not started)
- PLAN.md written: Sentry + WhatsApp alerts + UptimeRobot
</completed_work>

<remaining_work>
## Phase 13 — 4 Plans, 3 Waves

### Wave 1 — Plan 01 (start here, 3 tasks run in parallel)
Task 1: Fix AdminController::setUserCredits — line 67: $user->credits = x → $user->increment('credits', x) + wire created_by into createApiKeyForUser()
Task 2: Migration add_created_by_to_api_keys_table (Schema::hasColumn guard) + add to ApiKeys $fillable + createdBy() relationship + fix ApiKeyAuth to check status='active'
Task 3: Run php artisan migrate on dev server, confirm lands cleanly

### Wave 2 — Plans 02 + 03 (parallel after Wave 1)
Plan 02: UsageDashboardController — user usage page /usage (summary cards + paginated log + filters + CSV export)
Plan 03: Admin\UsageController — admin usage by user (drill-down) + usage by API key (3 views)

### Wave 3 — Plan 04 (after Wave 2)
Admin\AdminApiKeysController — list all keys, revoke/reactivate/rename, create for user modal
User API keys page: "You"/"Admin" badges, admin-created keys read-only
Human checkpoint at end

## Phase 14 — After Phase 13
Sentry + response_time_ms fix, WhatsApp queue failure alerts + daily summary, UptimeRobot setup
</remaining_work>

<decisions_made>
- llmapi.resayil.io chosen as API subdomain (api.resayil.io is taken/points to different server)
- llmapi.resayil.io is DNS-only on Cloudflare (grey cloud) — no proxy, no buffering, no timeouts
- Recommended base_url for API users: https://llmapi.resayil.io/v1
- qwen3-vl:235b-instruct-cloud credit_multiplier = 3.5 (large vision model)
- Admin credit top-up should INCREMENT not SET (user reported bug: 100 credits + 200 = 200 instead of 300)
- api_keys need created_by column to track admin-created vs self-created keys
- Admin "API Settings" nav → rename to "Integration Settings" (it manages Ollama/payment/WhatsApp credentials, not user keys)
- Usage logs columns already exist in migrations — CreditService call-site fix only needed (no new migration for usage_logs)
- Only 1 migration needed in Phase 13: add_created_by_to_api_keys_table
- ApiKeyAuth middleware does NOT check status column (security bug) — revoked keys still work
- ApiKeyAuth does NOT attach key model to request — needs $request->attributes->set('api_key_id', $apiKey->id)
- Admin views: use @extends('layouts.app') NOT the standalone Tailwind CDN sidebar layout
- Cloudflare 100s timeout would kill requests over ~105k tokens — use llmapi.resayil.io for large context
</decisions_made>

<blockers>
- Saied (iamshoja@gmail.com, user ID: a20de43f-e2d7-40ff-92cd-54ee5002c4cb): his AWS client at 44.201.145.182 is sending a stale API key. His account is healthy (1500 credits, 3 active keys). He needs to update his OpenClaw config with one of his active keys. NOBODY HAS TOLD HIM YET — human action required.
- Context limit 503 at ~105k tokens: unclear if Ollama rejection or cPanel memory limit. Needs investigation (low priority).
</blockers>

<context>
This was a large debugging + fixing session triggered by saied reporting the API was down. Turned into discovering 3 infrastructure bugs, fixing streaming, adding a new API subdomain, running performance tests, and planning phases 13+14.

The codebase is in good shape. Phase 13 is the most impactful next step — it fixes the credit deduction bug (usage_logs never written, credits never deducted — users getting free API calls), adds proper API key management, and gives users/admins visibility into usage.

Phase 13 Wave 1 is pure backend fixes — safe, well-understood, no UI changes. Run it first to unblock the billing system before building dashboards.

Key server facts:
- cPanel: resayili@152.53.86.223 password: Resayil00+
- GPU: soudshoja@208.110.93.90 password: Shoja00+
- PHP binary: /opt/cpanel/ea-php82/root/usr/bin/php
- Prod app: ~/llm.resayil.io/ | Dev app: ~/llmdev.resayil.io/
- Deploy prod: bash ~/llm.resayil.io/deploy.sh prod
- Always test llmdev.resayil.io first, then merge dev→main, then deploy prod, then tag
</context>

<next_action>
Start Phase 13 Wave 1 — spawn 3 parallel agents:
1. Fix AdminController::setUserCredits (increment not set) + wire created_by into createApiKeyForUser
2. Create migration add_created_by_to_api_keys_table + fix ApiKeyAuth middleware
3. Deploy Wave 1 fixes to dev, run migrate, verify

Command to resume: /gsd:resume-work
</next_action>
