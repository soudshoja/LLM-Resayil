---
phase: 11-content-technical-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/web.php
  - resources/views/docs/index.blade.php
  - resources/views/docs/getting-started.blade.php
  - resources/views/docs/authentication.blade.php
  - resources/views/docs/models.blade.php
  - resources/views/docs/billing.blade.php
  - resources/views/docs/rate-limits.blade.php
  - resources/views/docs/error-codes.blade.php
autonomous: true
requirements:
  - SEO-11-01: Expand /docs from 737 → 2,500+ words with code examples
  - SEO-11-02: Implement hreflang for EN/AR versions on all pages
  - SEO-11-03: Add breadcrumb schema to /docs subsections

must_haves:
  truths:
    - "/docs landing page displays 2,500+ words of API documentation"
    - "Each /docs subsection (getting-started, authentication, models, billing, rate-limits, error-codes) has 300+ words with code examples"
    - "All /docs pages render with valid JSON-LD breadcrumb schema"
    - "/docs/index links to all subsections with proper navigation"
  artifacts:
    - path: "resources/views/docs/index.blade.php"
      provides: "Documentation landing page with navigation structure"
      min_lines: 80
    - path: "resources/views/docs/getting-started.blade.php"
      provides: "Getting started section with code examples"
      contains: "cURL", "authentication", "token"
    - path: "resources/views/docs/authentication.blade.php"
      provides: "API key authentication documentation"
      contains: "Authorization header", "Bearer token"
    - path: "resources/views/docs/models.blade.php"
      provides: "Available models/endpoints documentation"
      contains: "model names", "capabilities"
    - path: "resources/views/docs/billing.blade.php"
      provides: "Billing and credits documentation"
      contains: "token consumption", "pricing"
    - path: "resources/views/docs/rate-limits.blade.php"
      provides: "Rate limiting and quotas"
      contains: "requests per minute", "quota reset"
    - path: "resources/views/docs/error-codes.blade.php"
      provides: "Error handling documentation"
      contains: "401", "429", "500", "error responses"
  key_links:
    - from: "routes/web.php"
      to: "resources/views/docs/*.blade.php"
      via: "Route definitions for /docs/*, GET only"
      pattern: "Route::get.*docs"
    - from: "resources/views/docs/index.blade.php"
      to: "subsection pages"
      via: "navigation links"
      pattern: "href.*getting-started|authentication|models"
    - from: "docs pages"
      to: "JSON-LD schema"
      via: "@json() helper for breadcrumbs"
      pattern: "@json.*breadcrumb"

---

<objective>
Expand /docs from current 737 words to 2,500+ words with code examples, implement breadcrumb schema on subsections, and create a comprehensive documentation landing page with proper navigation.

Purpose: Double documentation word count, provide hands-on code examples for developers, enable structured data crawling for SEO, establish /docs as comprehensive resource discovery point.

Output: 7 Blade template files (1 landing + 6 subsections) with 300+ words each, JSON-LD breadcrumb schema on all subsections, fully linked navigation structure.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md

Implementation will follow Laravel Blade conventions, use existing SeoHelper.php for schema generation, maintain dark luxury design system from app.blade.php layouts.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/Http/Controllers/PageController.php — contains current /docs controller logic
@resources/views/docs/ — current documentation directory (if exists)
@resources/views/layouts/app.blade.php — design system (colors, fonts, structure)

Key context from Phase 10 completion:
- SeoHelper.php exists with schema generation utilities
- Design system established: bg #0f1115, gold #d4af37, card #13161d
- Google Analytics already implemented with @if(app()->isProduction())
- All pages use proper <html lang=""> and dir="" attributes

Documentation expansion roadmap (from /seo skill):
- Getting Started: 350 words (setup, auth, first request)
- Authentication: 400 words (API key, Bearer token, error handling)
- Models: 450 words (available models, capabilities, cloud vs local)
- Billing & Credits: 350 words (token consumption rates, top-up, limits)
- Rate Limits: 300 words (request limits, quotas, reset timing)
- Error Codes: 300 words (common errors, troubleshooting, status codes)
- Code Examples: 400 words (cURL, JavaScript, Python examples)

Total: 2,550 words across 7 pages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /docs landing page with navigation and overview</name>
  <files>resources/views/docs/index.blade.php, routes/web.php</files>
  <action>
Create comprehensive documentation landing page at /docs that:
1. Displays brief overview of API documentation (150-200 words)
2. Lists all 6 subsections with brief descriptions (1-2 sentences each)
3. Includes quick-start cURL example (5-10 lines showing basic request)
4. Has clear "Next Steps" section linking to Getting Started subsection
5. Maintains dark luxury design: gold accent headers, card-based layout for subsections
6. Uses responsive Tailwind grid (2 cols on lg, 1 on mobile)
7. Includes breadcrumb navigation (Home → Documentation)
8. Add route in web.php: Route::get('/docs', [PageController::class, 'docs'])

Design: Hero section with title "API Documentation", brief intro, grid of 6 cards (each subsection), each card clickable linking to subsection page.
Use Tajawal font for Arabic, Inter for English.

Avoid: Mixing code snippets inside prose (separate into code blocks), hardcoding model names (use config values).
  </action>
  <verify>
    <automated>curl -s http://llmdev.resayil.io/docs | grep -q "Getting Started" && echo "✓ Landing page exists with subsection links"</automated>
  </verify>
  <done>
- Route /docs exists and returns 200
- Page displays hero section with documentation overview
- All 6 subsections visible in card grid with links
- Breadcrumb navigation present
- Dark luxury styling applied (gold headers, card backgrounds)
- Responsive on mobile (single column)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /docs/getting-started with setup and first request examples</name>
  <files>resources/views/docs/getting-started.blade.php, routes/web.php</files>
  <action>
Create getting-started guide at /docs/getting-started (350+ words) with:

Structure (in order):
1. "What is LLM Resayil?" intro (50 words) — explain what the API is
2. "Getting Your API Key" section (75 words + screenshot guidance):
   - Step 1: Register/login at https://llm.resayil.io
   - Step 2: Navigate to dashboard API keys section
   - Step 3: Copy your API key
3. "Your First Request" section (150+ words + code examples):
   - Explain Authorization header format: "Bearer YOUR_API_KEY"
   - Show cURL example (10 lines):
     ```
     curl -X POST https://llm.resayil.io/api/v1/chat/completions \
       -H "Authorization: Bearer YOUR_API_KEY" \
       -H "Content-Type: application/json" \
       -d '{
         "model": "mistral",
         "messages": [{"role": "user", "content": "Hello"}],
         "max_tokens": 100
       }'
     ```
   - Explain request fields: model, messages, max_tokens
   - Show example response (7 lines, pretty-printed)
4. "What's Next?" section (50 words) — link to auth, models, code examples pages

Code formatting: Use <pre><code class="language-bash"> for all code blocks
Maintain word count: 350-400 words total (not including code blocks)
Add route: Route::get('/docs/getting-started', [PageController::class, 'docsGettingStarted'])

Avoid: Hardcoding model names, using localhost URLs, mentioning internal GPU server.
  </action>
  <verify>
    <automated>curl -s http://llmdev.resayil.io/docs/getting-started | grep -q "First Request" && echo "✓ Getting started page created"</automated>
  </verify>
  <done>
- Route /docs/getting-started exists and returns 200
- Page displays 350+ words of content
- Contains "Bearer token" authorization explanation
- At least 1 cURL example visible
- Example response JSON shown
- Links to authentication section visible
- Breadcrumb navigation shows: Home → Documentation → Getting Started
  </done>
</task>

<task type="auto">
  <name>Task 3: Create /docs/authentication with API key and token handling</name>
  <files>resources/views/docs/authentication.blade.php, routes/web.php</files>
  <action>
Create authentication guide at /docs/authentication (400+ words) with:

Structure:
1. "API Key Authentication" section (150 words):
   - Explain how API keys work (persistent, account-specific)
   - Show where to find/generate keys in dashboard
   - Explain Bearer token format in Authorization header
   - Include cURL example with Bearer token
2. "API Key Lifecycle" section (100 words):
   - Key generation process
   - Best practices: never commit keys to git, rotate regularly
   - How to revoke a key in dashboard
3. "Authorization Header Format" section (75 words):
   - Exact header format: "Authorization: Bearer {key}"
   - Show it in cURL, JavaScript fetch, Python requests
   - Case sensitivity note
4. "Error Handling" section (75 words):
   - 401 Unauthorized (missing/invalid key)
   - 403 Forbidden (key revoked)
   - How to diagnose auth errors
   - Link to error-codes page

Code examples: Show same request in 3 languages (cURL, JavaScript, Python) for header format
Maintain: 400+ words, no code blocks in prose

Add route: Route::get('/docs/authentication', [PageController::class, 'docsAuthentication'])

Avoid: Exposing actual admin keys, mentioning internal auth middleware implementation details.
  </action>
  <verify>
    <automated>curl -s http://llmdev.resayil.io/docs/authentication | grep -q "Bearer token" && echo "✓ Authentication page created"</automated>
  </verify>
  <done>
- Route /docs/authentication exists and returns 200
- Page displays 400+ words of content
- Explains "Bearer token" format with examples
- Shows code examples in 2+ languages
- 401/403 error codes mentioned
- Breadcrumb navigation present
- Links to error-codes page visible
  </done>
</task>

<task type="auto">
  <name>Task 4: Create /docs/models with available models and capabilities</name>
  <files>resources/views/docs/models.blade.php, routes/web.php</files>
  <action>
Create models documentation at /docs/models (450+ words) with:

Structure:
1. "Available Models" section (200 words):
   - List 3-5 popular models (mistral, llama2, neural-chat, deepseek, qwen)
   - For each: name, provider, context window, capabilities summary
   - Use table format: Model | Provider | Context | Best For
   - Keep internal names/cloud labels hidden (show clean names to users only)
2. "Model Selection Guide" section (150 words):
   - Use mistral for: speed, low latency, general tasks
   - Use llama2 for: reasoning, longer context, complex queries
   - Use neural-chat for: conversation-specific tasks
   - Explain tradeoffs: speed vs accuracy, context window vs cost
3. "Model Capabilities" section (100 words):
   - All models support: text completion, chat interface, system prompts
   - Streaming vs non-streaming (note: streaming available)
   - Max output tokens per model
   - Token consumption rates (approximate)

Format: Table + paragraphs, NOT bullet lists
Code: Include example request showing model parameter (JSON)
Metadata: Link to billing page for token consumption rates

Add route: Route::get('/docs/models', [PageController::class, 'docsModels'])

Avoid: Exposing "cloud", ":cloud" suffix, "local GPU" terminology. Show only clean model names.
  </action>
  <verify>
    <automated>curl -s http://llmdev.resayil.io/docs/models | grep -q "mistral\|llama" && echo "✓ Models page created"</automated>
  </verify>
  <done>
- Route /docs/models exists and returns 200
- Page displays 450+ words of content
- Lists 3+ model names with brief descriptions
- Shows model capabilities (context, speed, use cases)
- Table showing Model | Provider | Context format visible
- No internal labels ("cloud", ":cloud", "local GPU") exposed
- Breadcrumb navigation present
- Link to billing page visible
  </done>
</task>

<task type="auto">
  <name>Task 5: Create /docs/billing with token consumption and rate information</name>
  <files>resources/views/docs/billing.blade.php, routes/web.php</files>
  <action>
Create billing guide at /docs/billing (350+ words) with:

Structure:
1. "How Credits Work" section (100 words):
   - Credits = purchased via top-up
   - Each API call consumes credits based on tokens (prompt + completion)
   - Show: 1 credit = 1,000 tokens (approximate, adjust to actual config)
   - Cost varies by model (some cheaper than others)
2. "Token Consumption" section (125 words):
   - Explain prompt_tokens (user input)
   - Explain completion_tokens (model output)
   - Show cost calculation: (prompt_tokens + completion_tokens) / 1000 = credits used
   - Show example: "A 100-token prompt + 200-token completion = 0.3 credits"
   - Link to models page for per-model rates
3. "Viewing Your Usage" section (75 words):
   - Dashboard → Billing shows current credits
   - Transaction history available
   - API response includes token counts (helpful for budgeting)
4. "Top-Up and Subscriptions" section (50 words):
   - Link to dashboard for subscription options
   - Payment via MyFatoorah
   - Instant credit application

Code example: Show API response with tokens_used field
Format: Paragraphs + example response JSON

Add route: Route::get('/docs/billing', [PageController::class, 'docsBilling'])

Avoid: Exposing actual credit rates (use generic "approximate"), hardcoding subscription prices.
  </action>
  <verify>
    <automated>curl -s http://llmdev.resayil.io/docs/billing | grep -q "credits\|tokens" && echo "✓ Billing page created"</automated>
  </verify>
  <done>
- Route /docs/billing exists and returns 200
- Page displays 350+ words of content
- Explains token consumption with example calculation
- Shows credit cost formula
- API response example visible
- Link to MyFatoorah or top-up section present
- Breadcrumb navigation shown
  </done>
</task>

<task type="auto">
  <name>Task 6: Create /docs/rate-limits with request quotas and reset timing</name>
  <files>resources/views/docs/rate-limits.blade.php, routes/web.php</files>
  <action>
Create rate limits guide at /docs/rate-limits (300+ words) with:

Structure:
1. "Rate Limiting Overview" section (100 words):
   - LLM Resayil applies rate limits to prevent abuse
   - Based on subscription tier (free, basic, pro, admin)
   - Limits reset at start of billing period
   - Exceeding limits returns 429 Too Many Requests
2. "Tier-Based Limits" section (125 words):
   - Create table: Tier | Requests/Min | Requests/Day | Max Tokens/Request
   - Free: 5 req/min, 100 req/day, 100 max tokens
   - Basic: 20 req/min, 500 req/day, 500 max tokens
   - Pro: 100 req/min, 5000 req/day, 2000 max tokens
   - Admin: 500 req/min, unlimited, 4000 max tokens
   - Note: Subscription tier shown in dashboard
3. "Handling Rate Limit Errors" section (75 words):
   - Check 429 response
   - Check X-RateLimit-Remaining header
   - Implement exponential backoff in client code
   - Example: wait 1s, then 2s, then 4s...
4. "Best Practices" section (50 words):
   - Batch requests when possible
   - Use caching to avoid repeat calls
   - Check remaining quota in headers
   - Upgrade subscription if hitting limits frequently

Code: Show example 429 response + backoff example (JavaScript)
Format: Paragraphs + table

Add route: Route::get('/docs/rate-limits', [PageController::class, 'docsRateLimits'])

Avoid: Hardcoding actual tier limits (use example values), exposing internal rate limiter implementation.
  </action>
  <verify>
    <automated>curl -s http://llmdev.resayil.io/docs/rate-limits | grep -q "429\|rate" && echo "✓ Rate limits page created"</automated>
  </verify>
  <done>
- Route /docs/rate-limits exists and returns 200
- Page displays 300+ words of content
- Explains 429 Too Many Requests status code
- Shows tier-based limits table (at least 2 tiers)
- Includes X-RateLimit-Remaining header explanation
- Backoff strategy mentioned
- Breadcrumb navigation shown
  </done>
</task>

<task type="auto">
  <name>Task 7: Create /docs/error-codes with common errors and troubleshooting</name>
  <files>resources/views/docs/error-codes.blade.php, routes/web.php</files>
  <action>
Create error codes reference at /docs/error-codes (300+ words) with:

Structure:
1. "Understanding Error Responses" section (75 words):
   - All errors return JSON: { error: { code, message, details } }
   - HTTP status code indicates error type
   - Example 400 vs 401 vs 500
2. "Common Status Codes" section (200+ words):
   - Create table: Status | Name | Cause | Solution
   - 400 Bad Request: malformed JSON, missing required field
   - 401 Unauthorized: missing/invalid API key
   - 403 Forbidden: authenticated but not authorized
   - 429 Too Many Requests: rate limit exceeded
   - 500 Internal Server Error: server issue
   - For each: 2-3 lines of explanation + troubleshooting tip
3. "Error Response Example" section (25 words):
   - Show JSON error response structure (pretty-printed)
4. "Debugging Checklist" section (50 words):
   - Is API key correct?
   - Is Authorization header present?
   - Is JSON valid?
   - Are required fields included?
   - Check X-RateLimit-Remaining header
   - Check /status page for outages

Code: Show 2-3 example error responses (400, 401, 429)
Format: Table + JSON examples

Add route: Route::get('/docs/error-codes', [PageController::class, 'docsErrorCodes'])

Avoid: Exposing stack traces, internal error codes, server implementation details.
  </action>
  <verify>
    <automated>curl -s http://llmdev.resayil.io/docs/error-codes | grep -q "401\|429\|500" && echo "✓ Error codes page created"</automated>
  </verify>
  <done>
- Route /docs/error-codes exists and returns 200
- Page displays 300+ words of content
- Shows error response JSON structure
- Status codes 400, 401, 403, 429, 500 explained
- Debugging checklist visible
- Troubleshooting tips provided
- Breadcrumb navigation shown
  </done>
</task>

<task type="auto">
  <name>Task 8: Add breadcrumb JSON-LD schema to all /docs subsections</name>
  <files>resources/views/docs/index.blade.php, resources/views/docs/getting-started.blade.php, resources/views/docs/authentication.blade.php, resources/views/docs/models.blade.php, resources/views/docs/billing.blade.php, resources/views/docs/rate-limits.blade.php, resources/views/docs/error-codes.blade.php</files>
  <action>
Add JSON-LD breadcrumb schema to all /docs pages (landing + 6 subsections) for SEO crawlability:

For each page, add in <head> section before closing tag:
```
@php
  $breadcrumbs = [
    ['name' => 'Home', 'url' => route('home')],
    ['name' => 'Documentation', 'url' => route('docs.index')],
    ['name' => $pageTitle, 'url' => url()->current()]  // e.g., "Getting Started"
  ];

  $schema = [
    '@context' => 'https://schema.org',
    '@type' => 'BreadcrumbList',
    'itemListElement' => array_map(function($item, $key) {
      return [
        '@type' => 'ListItem',
        'position' => $key + 1,
        'name' => $item['name'],
        'item' => $item['url']
      ];
    }, $breadcrumbs, array_keys($breadcrumbs))
  ];
@endphp

<script type="application/ld+json">
  @json($schema)
</script>
```

Apply to all 7 pages:
- /docs → breadcrumb: Home → Documentation
- /docs/getting-started → breadcrumb: Home → Documentation → Getting Started
- /docs/authentication → breadcrumb: Home → Documentation → Authentication
- /docs/models → breadcrumb: Home → Documentation → Models
- /docs/billing → breadcrumb: Home → Documentation → Billing
- /docs/rate-limits → breadcrumb: Home → Documentation → Rate Limits
- /docs/error-codes → breadcrumb: Home → Documentation → Error Codes

Verify: Use Google's Structured Data Testing Tool (paste page URLs)
Note: Schema will only be visible in page source (view-source:), not in rendered HTML

Avoid: Hardcoding URLs (use route() helper), exposing internal route names.
  </action>
  <verify>
    <automated>curl -s http://llmdev.resayil.io/docs/getting-started | grep -q "schema.org.*BreadcrumbList" && echo "✓ Breadcrumb schema present"</automated>
  </verify>
  <done>
- All 7 /docs pages contain valid JSON-LD breadcrumb schema
- Breadcrumbs show correct hierarchy (Home → Documentation → Page Title)
- Schema validates at https://validator.schema.org (0 errors)
- Markup visible in page source (view-source)
- All route() helpers resolve correctly (no broken links)
- Breadcrumbs use correct position numbers (1, 2, 3...)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All 7 routes exist and return 200: /docs, /docs/getting-started, /docs/authentication, /docs/models, /docs/billing, /docs/rate-limits, /docs/error-codes
2. Total word count across all pages: 2,500+ words (minimum 300+ per subsection)
3. Code examples present on at least 4 pages: getting-started, authentication, models, error-codes
4. Breadcrumb navigation visible on all subsections (Home → Documentation → Page Title)
5. JSON-LD breadcrumb schema validates on all 7 pages
6. Design system applied: dark luxury (gold headers, card backgrounds, Inter/Tajawal fonts)
7. No hardcoded model names, internal labels, or admin email addresses exposed
8. Responsive layout works on mobile (single column) and desktop (multi-column)
</verification>

<success_criteria>
**Documentation Expansion Complete:**
- [ ] /docs landing page displays with navigation grid to all 6 subsections
- [ ] Each subsection page (getting-started, authentication, models, billing, rate-limits, error-codes) displays 300+ words
- [ ] Code examples visible on getting-started, authentication, error-codes pages
- [ ] All 7 pages include breadcrumb navigation showing Home → Documentation → [Page]
- [ ] JSON-LD breadcrumb schema validates on all pages (0 schema errors)
- [ ] Total documentation word count: 2,500+ words across all pages
- [ ] No internal labels exposed (no "cloud proxy", ":cloud suffix", "local GPU")
- [ ] Mobile responsive layout verified (single column on small screens)
- [ ] All routes return 200 status code
- [ ] Dark luxury design applied consistently (gold accents, dark bg, proper fonts)
</success_criteria>

<output>
After completion, create `.planning/phases/11-content-technical-seo/11-01-SUMMARY.md` documenting:
- All 7 route definitions created
- Word count per page (with totals)
- Code examples added (types and locations)
- Schema validation results
- Design system application notes
- Any deviations from spec and why
</output>
